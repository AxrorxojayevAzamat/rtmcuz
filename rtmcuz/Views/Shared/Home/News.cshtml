@model IEnumerable<rtmcuz.Data.Models.Section>

<section class="news-section">
    <div class="container">
        <div class="head">
            <h3 class="title">@sharedLocalizer["news"]</h3>
            <a asp-action="Section" asp-route-slug="news" class="see-all">@sharedLocalizer["see_all"]</a>
        </div>
        <div class="news-grid">
            @foreach (var item in Model.Select((value, i) => new { i, value }))
            {
                @if (item.i == 0)
                {
                    <a asp-action="Show" asp-route-slugGroup="news" asp-route-slug="@item.value.Slug" class="news-item main">
                        <div class="bg">
                            <div class="bg-linear"></div>
                            <img class="home-news" src="/uploads/@item.value.Image.Path">
                        </div>
                        <div class="title">@item.value.Title</div>
                    </a>
                }
                else
                {
                    <a asp-action="Show" asp-route-slugGroup="news" asp-route-slug="@item.value.Slug" class="news-item">
                        <div class="bg">
                            <div class="bg-linear"></div>
                            <img class="home-news" src="/uploads/@item.value.Image.Path">
                        </div>
                        <div class="title">@item.value.Title</div>
                    </a>
                }

            }
        </div>
    </div>
</section>